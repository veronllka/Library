<Window x:Class="Library.Views.DatabaseBackupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:local="clr-namespace:Library.Views"
        mc:Ignorable="d"
        Title="Резервное копирование базы данных" Height="550" Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip" 
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/GlobalStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <!-- Упрощенные анимации -->
            <Storyboard x:Key="DialogEnterAnimation">
                <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                                From="0" To="1" Duration="0:0:0.2" />
            </Storyboard>
            
            <!-- Стиль для секундарной кнопки -->
            <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource GlassButton}">
                <Setter Property="Background">
                    <Setter.Value>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#8B5CF6" Offset="0"/>
                            <GradientStop Color="#7C3AED" Offset="1"/>
                        </LinearGradientBrush>
                    </Setter.Value>
                </Setter>
                <Setter Property="BorderBrush" Value="{StaticResource SecondaryColor}"/>
            </Style>
            
            <!-- Стиль для основной кнопки -->
            <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource GlassButton}">
                <Setter Property="Background" Value="{StaticResource PrimaryGradient}"/>
                <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource DialogEnterAnimation}"/>
        </EventTrigger>
    </Window.Triggers>
    
    <!-- Полупрозрачный фон -->
    <Grid>
        <Grid.Background>
            <SolidColorBrush Color="#AA000000"/>
        </Grid.Background>
        
        <Border Style="{StaticResource GlassCard}" Margin="20">
            <Grid>
                <!-- Заголовок -->
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Background="{StaticResource PrimaryGradient}" Grid.Row="0"
                       CornerRadius="16,16,0,0" Padding="20">
                    <Grid>
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="DatabaseSync" Width="32" Height="32"
                                                      Foreground="{StaticResource AccentColor}" Margin="0,0,12,0"/>
                            <StackPanel>
                                <TextBlock Text="Управление резервными копиями" FontSize="20" FontWeight="SemiBold"
                                          Foreground="{StaticResource TextPrimaryColor}"/>
                                <TextBlock Text="Создание и восстановление копий базы данных"
                                          FontSize="12" Foreground="{StaticResource TextSecondaryColor}"/>
                            </StackPanel>
                        </StackPanel>

                        <Button HorizontalAlignment="Right" Style="{StaticResource GlassButton}"
                               Click="CloseButton_Click">
                            <iconPacks:PackIconMaterial Kind="Close" Width="20" Height="20"/>
                        </Button>
                    </Grid>
                </Border>

                <!-- Основное содержимое -->
                <Grid Grid.Row="1" Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Раздел создания резервной копии -->
                    <Border Grid.Row="0" Style="{StaticResource GlassCard}" Padding="20" Margin="0,0,0,20">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="Создание резервной копии" 
                                      FontSize="18" FontWeight="SemiBold" 
                                      Foreground="{StaticResource TextPrimaryColor}"
                                      Margin="0,0,0,15"/>

                            <Grid Grid.Row="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBox x:Name="BackupPathTextBox" Grid.Column="0" 
                                         Style="{StaticResource GlassTextBox}" Margin="0,0,10,0"
                                         Text="{Binding BackupPath}"/>

                                <Button Grid.Column="1" Content="Обзор..." 
                                        Style="{StaticResource SecondaryButton}"
                                        Click="BrowseBackupButton_Click"/>
                            </Grid>

                            <Button Grid.Row="2" Content="Создать резервную копию" 
                                    Style="{StaticResource SuccessButton}" 
                                    Margin="0,15,0,0" HorizontalAlignment="Left"
                                    Click="CreateBackupButton_Click"/>
                        </Grid>
                    </Border>

                    <!-- Раздел восстановления из резервной копии -->
                    <Border Grid.Row="1" Style="{StaticResource GlassCard}" Padding="20" Margin="0,0,0,20">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="Восстановление из резервной копии" 
                                      FontSize="18" FontWeight="SemiBold" 
                                      Foreground="{StaticResource TextPrimaryColor}"
                                      Margin="0,0,0,15"/>

                            <Grid Grid.Row="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBox x:Name="RestorePathTextBox" Grid.Column="0" 
                                         Style="{StaticResource GlassTextBox}" Margin="0,0,10,0"
                                         Text="{Binding RestorePath}"/>

                                <Button Grid.Column="1" Content="Обзор..." 
                                        Style="{StaticResource SecondaryButton}"
                                        Click="BrowseRestoreButton_Click"/>
                            </Grid>

                            <Button Grid.Row="2" Content="Восстановить из резервной копии" 
                                    Style="{StaticResource ErrorButton}" 
                                    Margin="0,15,0,0" HorizontalAlignment="Left"
                                    Click="RestoreButton_Click"/>
                        </Grid>
                    </Border>

                    <!-- Лог операций -->
                    <Border Grid.Row="2" Style="{StaticResource GlassCard}" Padding="10">
                        <ScrollViewer MaxHeight="100" VerticalScrollBarVisibility="Auto">
                            <TextBlock x:Name="LogTextBlock" TextWrapping="Wrap" 
                                      Foreground="{StaticResource TextSecondaryColor}"/>
                        </ScrollViewer>
                    </Border>
                </Grid>

                <!-- Нижние кнопки -->
                <Grid Grid.Row="2" Margin="20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock x:Name="StatusText" Text="Выберите действие" VerticalAlignment="Center"
                              Foreground="{StaticResource TextSecondaryColor}"/>

                    <Button Grid.Column="1" Style="{StaticResource PrimaryButton}"
                           Content="Закрыть" Click="CloseButton_Click"
                           Padding="25,10"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window> 